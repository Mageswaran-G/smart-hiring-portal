// constants.js
// One place for all fixed values
// Import this everywhere instead of repeating

const JOB_TYPES = ['internship', 'full-time', 'part-time', 'contract'];

const WORK_MODES = ['remote', 'onsite', 'hybrid'];

const EXPERIENCE_LEVELS = ['fresher', 'junior', 'mid', 'senior'];

const JOB_STATUS = ['draft', 'published', 'closed'];

module.exports = {
  JOB_TYPES,
  WORK_MODES,
  EXPERIENCE_LEVELS,
  JOB_STATUS,
};